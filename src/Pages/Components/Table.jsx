import React from 'react';
import styled from 'styled-components';

const Styles = styled.div`

`
 
function Table(props) {
    const getRow = (arr) => {
        let i = 0;
        while(i < arr.length - 3) {
            let row = arr.slice(i, i+2);
            
        }
    }

    return (
        <Styles>

        </Styles>
    )
}